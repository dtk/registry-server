import RegistryService from './registryService';

// async function instantiateAndregister() {
  let registryService = new RegistryService();
  // registryService.listenForRegistrations();
// }

// instantiateAndregister();